name: joke_generator
description: A random joke generator app using external APIs.
publish_to: 'none'

version: 0.1.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # API & Networking
  dio: ^5.3.0
  http: ^1.1.0
  
  # State Management
  provider: ^6.0.0
  
  # Utility
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
